#! /bin/bash

gitStatus()
{
	echo ' - - - - - - - - - - - - - - - - - - - - '
	git status
	return 0
}

git pull

if [ $# == 0 ] ; then
	gitStatus
	exit
fi

git pull
git add ./
git commit -am "$*"
git push
gitStatus
